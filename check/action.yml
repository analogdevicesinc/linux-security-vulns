name: Check CVEs
description: Run grondig

inputs:
  request:
    description: "query entries in json"
    required: true
  gh-token:
    description: "GitHub Token"
    default: ${{ github.token }}

outputs:
  result:
    description: 'list of cves per uid, in json'
    value: ${{ steps.check.outputs.result }}

runs:
  using: composite
  steps:
    - name: Restore cache
      uses: actions/cache/restore@v5
      id: cache
      with:
        key: linux-security-vulns
        path: |
          verhaal.db
          post.db
          grondig
        fail-on-cache-miss: false

    - name: Get tools and databases
      if: steps.cache.outputs.cache-hit != 'true'
      shell: bash
      run: |
        curl -sL -H "Authorization: Bearer ${{ inputs.gh-token }}" -o verhaal.db.xz \
            https://github.com/sashalevin/verhaal/releases/download/db-latest/verhaal.db.xz
        xz --decompress verhaal.db.xz

        curl -sL -H "Authorization: Bearer ${{ inputs.gh-token }}" -o post.db.xz \
            https://github.com/analogdevicesinc/linux-security-vulns/releases/download/latest/post.db.xz
        xz --decompress post.db.xz

        curl -sL -H "Authorization: Bearer ${{ inputs.gh-token }}" -o grondig \
            https://github.com/analogdevicesinc/linux-security-vulns/releases/download/latest/grondig
        chmod +x grondig

    - name: Check
      id: check
      shell: bash
      run: |
        [[ "${{ runner.debug }}" == "1" ]] && set -x

        result=$(printf '%s' '${{ inputs.request }}' | \
            ./grondig --post-db ./post.db --verhaal-db ./verhaal.db)

        echo "$result"

        echo 'result<<EOF' >> $GITHUB_OUTPUT
        echo "$result" >> $GITHUB_OUTPUT
        echo 'EOF' >> $GITHUB_OUTPUT

    - name: Save cache
      if: steps.cache.outputs.cache-hit != 'true'
      uses: actions/cache/save@v5
      with:
        key: linux-security-vulns
        path: |
          verhaal.db
          post.db
          grondig
